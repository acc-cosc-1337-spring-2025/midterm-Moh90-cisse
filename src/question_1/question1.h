#ifndef PRIME_UTILS_H
#define PRIME_UTILS_H

#include <vector>

// Function prototype to check if a number is prime
bool is_prime(int num);

// Function prototype to get all prime numbers up to 'num'
std::vector<int> get_primes(int num);

#endif // PRIME_UTILS_H
